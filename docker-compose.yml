version: '3'

networks:
  playground-network:
    driver: bridge

services:
  neo4j:
    image: neo4j:4.1.1
    container_name: neo4j
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - ./images/neo4j/data:/var/lib/neo4j/data
      - ./images/neo4j/plugins:/var/lib/neo4j/plugins
      - ./images/neo4j/conf:/var/lib/neo4j/conf
      - ./images/neo4j/import:/var/lib/neo4j/import
    environment:
      - NEO4J_AUTH=none
    networks:
      - playground-network

  notebook:
    build:
      context: images/jupyter
    container_name: jupyter-notebook
    links:
      - neo4j:neo4j
    ports:
      - "8888:8888"
    volumes:
      - ./images/jupyter/notebooks:/playground/notebooks
    networks:
      - playground-network
